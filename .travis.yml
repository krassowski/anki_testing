dist: xenial
language: python
sudo: required

python:
  - 3.7

install: 
  - pip install pyqt5==5.14
  - pip install anki aqt # install both Anki modules from PyPI
  - source setup.sh

script:
  - pytest
